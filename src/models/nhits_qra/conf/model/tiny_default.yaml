# conf/model/tiny.yaml
model_type: nhits_qra
size: tiny
plot_size: tiny
input_size: ${data.context_length}
h: ${data.forecast_horizon}

# capacity
n_blocks: [2, 2]
mlp_units:
  - [16, 16]
  - [16, 16]
dropout_prob_theta: 0.1
n_pool_kernel_size: [4,2]
n_freq_downsample: [4,2]

past_unknown_cov_cutoff: 14
realistic_mode: true