defaults:
  - data: ???
  - model: ???          # override on CLI: model=small/base
  - features: feature_registry   # override: features=calendar_all
  - train_nhits: ???
  - train_qra: ???
  - logging: default
  - _self_

experiment: ???

model_type: nhits_qra
seed: 42
resume: false                 # resume from last.ckpt if present
runs_root: ${hydra:runtime.cwd}/outputs/${model_type}/${model.size}/${experiment}/runs
out_root: outputs/${model_type}/${model.size}/${experiment}
train:
  run_id: null

# Make Hydra put the working dir for this run *inside* that structure
hydra:
  run:
    dir: ${out_root}
  job:
    chdir: true